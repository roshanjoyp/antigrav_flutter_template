name: craft_generator
description: >-
  Composes buyer-specific zips of the CRAFT Flutter template: selects
  modules, strips generation markers, renames, verifies, and packages.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  archive: ^4.0.2
  glob: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
